class Pratos {
  title: string
  image: string
  desc: string
  id: number

  constructor(title: string, image: string, desc: string, id: number) {
    this.title = title
    this.image = image
    this.desc = desc
    this.id = id
  }
}

export default Pratos
